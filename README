twrap
=====

Text wrapper utility, wraps text from stdin into stdout.

Installation
------------

Configured only for UNIX systems for the time being:

    sudo make clean install

Or just build it locally

    make

Examples
--------

Wraps the roff file mentioned:

    cat twrap.1 | ./twrap
